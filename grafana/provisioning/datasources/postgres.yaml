apiVersion: 1

datasources:
  - name: Postgres
    uid: postgres-cmdb
    type: postgres
    access: proxy
    url: postgres:5432
    database: cmdb
    user: cmdb
    secureJsonData:
      password: cmdb123
    jsonData:
      sslmode: disable
      maxOpenConns: 10
      maxIdleConns: 5
      connMaxLifetime: 14400
    isDefault: false
    editable: true
